
import { Metadata } from 'next';
import React from 'react'

export const metadata: Metadata = {
    title: "Setting | Template",
};

export default function page() {
    return (
        <h2 className="text-center mt-12">Setting</h2>
    )
}
